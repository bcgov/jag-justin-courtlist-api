name: Justin Courtlist API - Promote to Prod
permissions:
  contents: write
on:
  workflow_dispatch:
env:
  IMAGE_NAME: justin-courtlist
  WORKING_DIRECTORY: .
  BRANCH_NAME: main
  IMAGE_TAG: prod

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Login to Artifactory
        uses: docker/login-action@v1
        with:
          registry: artifacts.developer.gov.bc.ca
          username: ${{ secrets.ARTIFACTORY_USERNAME }}
          password: ${{ secrets.ARTIFACTORY_PASSWORD }}

      - name: Get short SHA
        id: short_sha
        run: |
          echo "::set-output name=SHORT_SHA::$(git rev-parse --short HEAD)"
          echo "Short SHA: $SHORT_SHA" 

      - name: Docker Backup Prod Courtlist Image
        run: |
          docker pull artifacts.developer.gov.bc.ca/jwub-justin/${{ env.IMAGE_NAME }}:prod
          docker tag artifacts.developer.gov.bc.ca/jwub-justin/${{ env.IMAGE_NAME }}:prod artifacts.developer.gov.bc.ca/jwub-justin/${{ env.IMAGE_NAME }}:prod-backup
          docker push artifacts.developer.gov.bc.ca/jwub-justin/${{ env.IMAGE_NAME }}:prod-backup

      - name: Docker Pull Test Courtlist Image
        run: |
          docker pull artifacts.developer.gov.bc.ca/jwub-justin/${{ env.IMAGE_NAME }}:test
          docker tag artifacts.developer.gov.bc.ca/jwub-justin/${{ env.IMAGE_NAME }}:test artifacts.developer.gov.bc.ca/jwub-justin/${{ env.IMAGE_NAME }}:${{ env.IMAGE_TAG }}

      - name: Docker Push Prod Courtlist to Artifactory
        id: publish
        run: |
          docker push artifacts.developer.gov.bc.ca/jwub-justin/${{ env.IMAGE_NAME }}:${{ env.IMAGE_TAG }}

      - name: Set GitOps SSH Key
        uses: webfactory/ssh-agent@v0.5.4
        with:
          ssh-private-key: ${{ secrets.COURTLIST_SSH_KEY }}

      - name: Update Helm Values and Commit
        id: helm
        run: |
          
          # Commit and push the changes
          git config --global user.email "actions@github.com"
          git config --global user.name "Courtlist GitHub Actions"
          git clone -b ${{ env.BRANCH_NAME }} --single-branch git@github.com:bcgov-c/tenant-gitops-wuboml.git
          
          # Navigate to the directory containing your Helm values file for the environment develop -> DEV, test -> test
          cd tenant-gitops-wuboml/charts

          # Update the Helm values file with the new image tag and version
          DATETIME=$(date +'%Y-%m-%d %H:%M:%S')  # Get current date and time

          sed -i "s/cltag: .*/cltag: ${{ env.IMAGE_TAG }} # Image Updated on $DATETIME/" ../deploy/${{ env.IMAGE_TAG }}_values.yaml
          sed -i "s/clversion: .*/clversion: ${{ steps.short_sha.outputs.SHORT_SHA }} # Version Updated on $DATETIME/" ../deploy/${{ env.IMAGE_TAG }}_values.yaml
          
          git add .
          
          git add ../deploy/${{ env.IMAGE_TAG }}_values.yaml
          
          # Repackage Helm Chart      

          cd gitops

          helm dependency build
          
          cd charts

          git add .
          
          git commit -m "Update Courtlist image tag"
          git push origin ${{ env.BRANCH_NAME }} # Update the branch name as needed
