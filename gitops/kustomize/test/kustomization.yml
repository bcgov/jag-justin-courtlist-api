kind: Kustomization
apiVersion: kustomize.config.k8s.io/v1beta1

resources:
  - ../base

patchesStrategicMerge:
  - deployment.yml
#  - route.yml

namespace: wuboml-prod

images:
  - name: image-registry.openshift-image-registry.svc:5000/wuboml-tools/court-list-ws
    newTag: "test"

configMapGenerator:
  - name: court-list-ws-config
    behavior: merge
    literals:
      - COURTLIST_BASEURL='https://test.jag.gov.bc.ca/ords/tstj/justinords/web/'
      - KEYCLOAK_AUTH_SERVER_URL='https://keycloak-060d15-test.apps.silver.devops.gov.bc.ca/auth'

